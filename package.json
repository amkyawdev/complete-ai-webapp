{
  "name": "oh",
  "version": "1.0.0",
  "description": "AI Chat Platform with Cloudflare Workers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config infra/wrangler.toml",
    "deploy": "wrangler deploy --config infra/wrangler.toml",
    "deploy:prod": "wrangler deploy --config infra/wrangler.toml",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
